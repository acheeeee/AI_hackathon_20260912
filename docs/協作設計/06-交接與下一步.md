# 交接：第77條八款條件規則與逐款補值已完成

**最新更正（2026-09-13）：** 原先「八款完成」混用了畫面／API契約與條件判斷。現在八款皆有事實依賴的判斷、28欄型別定義、文件原文來源與每款編輯保存；305項後端／78項前端測試PASS，aggregate95%。舊案件可由已存文件補讀、人工值含null優先。第57條自不服表示起30日補送，不需先有通知。AWS固定網址及最終release gate見 [v0.2.4驗收](../../sysdoc/verification/article77-v024.md)。下方v0.2.3及230／70數字保留為歷史快照，不得當最新測試數。

日期：2026-09-13｜對象：接手匯出、部署或 §7.5 的下一位開發者｜前置閱讀：[README](README.md)、[01](01-人工操作與案件流程.md)、[02](02-AI側邊欄與查證編修.md)、[03](03-後端SQLite與版本合併.md)、[04](04-API契約.md)、[05](05-實作順序與驗收.md)、[07 AgentCore 部署](07-AgentCore部署與線上模型.md)

**如果你是接手下一輪的人，先讀完本文件全部八節再動手，尤其是 §7——草稿生成
（§7.1）、採用／手動編輯（§7.2）、選取叫 AI（§7.3：先解釋再局部修改／diff／
接受／拒絕）與 UI 打磨成五步驟精靈（§7.4）都已完成；本批追加的第77條八款
審查契約、可編輯擷取分析、法規結果展開／官方連結與正式草稿骨架見 §7.4a。
PDF 匯出已完成文字、結構與渲染驗收；AWS v0.2.3 已在固定 EIP 原地發布並通過
health、虛構文件 AgentCore 進件、草稿互動與 PDF live smoke。不要繞過既有 proposal → merge
preview → application 鏈，也不要因為 PDF 已完成就直接刪除仍承擔 legacy Word
匯出的舊後端。**

階段 A（A1 至 A3）、B0 證據底座、B1 run／事件持久化、B2 Evidence 工具 adapter、B3 固定模型端到端、線上 AgentCore provider，以及上傳建案、案件列表、原檔展開、側邊欄 AI 對話、程序審查、選法規、草稿生成、全文提案採用與手動編輯、選取叫 AI（解釋＋局部修改）、UI 打磨成五步驟精靈都已完成。本批再補齊八款審查輸出、進件分析、法規結果與草稿格式契約。本文件說明已經有什麼、必須遵守什麼、下一輪做什麼，以及哪些決策已確定。逐案驗收狀態在 [05 §3 與 §6](05-實作順序與驗收.md)，不要在本文件另建一份狀態表；AgentCore 部署細節在 [07](07-AgentCore部署與線上模型.md)，不要在這裡重複。

**目前權威快照（2026-09-13）：** 本機後端 230 passed／`caseapi` aggregate coverage
95%，前端 70 passed，type check、production build、oxlint、eslint 全部 PASS。法規選擇
現在由伺服器以 r3 `chunk_id` 重新驗證並重建 canonical metadata，前端帶入的法規名稱、
條號或摘要不再能污染已保存選擇與後續草稿。進件的 AgentCore 外送改成逐次、預設未
勾選的明確同意；未同意只跑本機分析，線上失敗也會保存案件並回退本機。兩份完全
虛構的上傳文件位於 `data/demo/`。法律覆核、TLS、登入與多人權限仍未完成；AWS
v0.2.3 的完成狀態已有部署後 health／upload／UI／PDF live smoke 證據。

## 1. 先確認你在哪裡

```bash
cd backend && .venv/bin/python -m pytest              # 應為 230 passed，caseapi aggregate 覆蓋率 95%
cd frontend && npx vitest run                          # 應為 70 passed
.venv_pre/bin/python -m scripts.preprocess.run validate --release r3  # 應為 15/15 PASS（若無 .venv_pre，代表本機未建立前處理環境，非回歸）
.venv_pre/bin/python -m tests.preprocess.test_statute_layout           # 應為 4/4 PASS
backend/scripts/run_api.sh                             # 啟動後端；會載入 .env，啟動時印出實際 provider
```

**啟動後端一律用 `backend/scripts/run_api.sh`，不要直接跑 `uvicorn caseapi.main:app`。**
`config.py` 只讀 `os.environ`、不會自己去讀 `.env`（這是刻意的，否則
`test_model_provider_selection.py` 會被真實 `.env` 污染），所以直接跑 uvicorn 會讓
`.env` 裡的 `CASEAPI_MODEL_PROVIDER=agentcore` 完全失效、**默默退回固定假模型**——
「問 AI」就只會回佔位字串，看起來像功能壞掉（2026-09-13 實際踩過）。腳本用 uvicorn
內建的 `--env-file` 載入，並在啟動時把實際 provider 印出來。

`http://127.0.0.1:8001/api/v1/docs` 是自動產生的 OpenAPI 文件，目前 30 個路徑、36 個 operation（新增 `POST /cases/{case_id}/draft-generations`；`POST /drafts` 與 `POST /proposals` 已標 `deprecated`）。

### 上一輪（Claude Code，2026-09-13）啟動設定與範圍——已完成

本機已確認 Claude Code 2.1.252 支援 `--model` 與 `--effort`。從專案根目錄啟動：

```bash
claude --model opus --effort high --permission-mode acceptEdits
```

該輪只做 **§7.3 選取叫 AI（先解釋，再做修改／diff／接受／拒絕）** 與
**§7.4 UI 打磨**，兩者都已完成；沒有動 §7.5 或刪 legacy。每個小批次都遵守：先新增
一個會失敗的行為測試並提交 RED，再做最小實作到 GREEN，跑完整相關測試，更新
05／06，最後各自獨立提交，實際拆成六顆 commit（7.3a／7.3b／7.4 各自的 RED＋GREEN）。
未使用 `--dangerously-skip-permissions`。細節與實跑證據見 §7.3、§7.4 與
[05 §6](05-實作順序與驗收.md)。

**下一輪只做 §7.5**（見本文件 §7.5）：先確認 DOCX 匯出等價功能存在或已向使用者
明確確認不需要，才能刪除舊後端 `backend/api.py`／`backend/src/` 與前端
`WizardView.vue`／`demoCases.ts` 等 legacy 檔案。

**前端 dev server 放久了會壞：** 2026-09-13 遇到一個跑了一小時以上的 `npm run dev`，`GET /api/v1/*` 正常但 `PUT` 透過 proxy 直接連線重置（curl 回 000、瀏覽器回 `Failed to fetch`），打 `:8001` 本身完全正常。重啟 dev server 就好。看到「儲存失敗」但後端 log 連請求都沒收到時，先重啟 dev server 再查程式。

環境：Python 3.12.14，venv 在 `backend/.venv`，執行時依賴 `backend/requirements.txt`，測試依賴 `backend/requirements-dev.txt`（pytest、pytest-cov、httpx）。**不要另建 venv，也不要在未經使用者同意下安裝新套件。**

資料庫預設 `backend/data/caseapi.db`，已在 `.gitignore`。用 `CASEAPI_DB_PATH` 換位置；`CASEAPI_ACTOR_ID` 換 stub 身分；`CASEAPI_EVIDENCE_RELEASE_DIR`／`CASEAPI_EVIDENCE_RELEASE_ID` 換證據 release（預設 r3）。

舊後端 `backend/api.py` 與 `backend/src/` **目前完全沒有改動**，現有 Vue 前端仍靠它跑離線示範。使用者已決定新舊不並存到底：前端會逐畫面改接新後端，每改完一個畫面就能繼續錄 demo，全部改完才刪舊後端與舊前端的 demo 選單，細節見 §6。改接完成前不要為了整併去動舊的。

## 2. 程式地圖

```text
backend/caseapi/
  main.py            建立 app、掛 router、註冊例外處理、啟動時跑 migration、組裝 model_provider／evidence_repository
  config.py          Settings（db_path、actor_id、evidence_release_dir、evidence_release_id）
  envelope.py        {success, data, error, meta} 的組裝
  errors.py          ApiError 與各錯誤碼的建構函式
  idempotency.py     冪等鍵查詢與保存
  audit.py           每案 append-only 稽核鏈（prev_hash → entry_hash）
  ids.py, clock.py   不透明 ID、UTC RFC 3339 時間
  db/connection.py   連線（foreign_keys、WAL、busy_timeout）與 transaction()
  db/migrations.py   migration 執行器；SQL 在 db/migrations/*.sql
  db/migrations/004_ai_runs.sql  ai_runs、jobs、run_events 與狀態／事件約束
  db/migrations/005_chat.sql     chat_threads、messages
  db/migrations/006_case_documents.sql 上傳的訴願書／行政處分函原始位元組與抽取快取
  domain/fact_fields.py  可人工修改的事實欄位 allowlist
  domain/appeal_extraction.py 訴願書（固定格式表單）規則式抽欄位；行政處分函刻意不抽
  services/intake_service.py 上傳建案：存 PDF 原件、跑抽取、種第一版 facts
  domain/appeal_deadline.py 訴願法第14條三十日期間計算，純函式，不碰 DB
  services/procedural_review_service.py 讀 facts、呼叫 appeal_deadline，不持久化
  services/statute_selection_service.py 選法規：真的 BM25 搜尋 r3 statute、保存人工挑選結果
  services/draft_generation_service.py 草稿生成：凍結這次生成的基底（必要時先建空殼草稿）與把結果寫成提案
  services/document_service.py 讀回 case_documents 的 metadata 與原始 bytes
  evidence/repository.py r3 release hash 驗證、離線 BM25、同案排除、原文開啟與 quote 核對
  tools/evidence_tools.py 四個 server-side tool、真實 activity、已開啟來源與 evidence 原子寫入
  ai/contracts.py    ModelProvider／ModelRequest／ModelResult／ToolGatewayLike 的 Protocol 邊界
  ai/fixed_provider.py 無網路、決定性的 verify／explain 固定回應，證明協調鏈可重現
  ai/agentcore_provider.py 線上 provider；本機工具查證結果當 context 送進 AgentCore 只求生成文字
  ai/selection_text.py 從 read_selection_context 結果取出可讀文字，draft_block／fact_field 兩種目標都處理，provider 共用
  ai/runner.py        ToolGateway（把工具呼叫綁死在單一 case/run）、start_run／fail_run 與 execute_chat_run 背景協調器
  ai/draft_runner.py  execute_draft_run：草稿生成 run 的背景協調器，結果寫成 proposal 不是聊天訊息
  ai/draft_context.py 依 run 凍結的 refs 讀出事實／選定法規／訴願書原文，交給 provider 的是資料不是 DB 連線
  ai/draft_composition.py 兩個 provider 共用的 draft 步驟：開選定法規的原文、排區塊、組 prompt context
  schemas/           請求與回應模型：case, facts, draft, annotation, target, evidence, proposal, run, chat
  services/          case_repository, case_service, resource_service, facts_service,
                     draft_service, annotation_service, proposal_service,
                     merge_service, apply_service, run_service, chat_service
  api/               deps, mutation（共用冪等流程）, routes_*（含 routes_chat、routes_evidence、routes_intake、routes_documents）

backend/agentcore_app/agent.py   部署到 AgentCore Runtime 的代理本身，不被 caseapi import，只被下面的部署腳本打包
backend/scripts/deploy_agentcore.py  一次性部署腳本，會建立真實 AWS 資源；不進 pytest，細節見 07
```

六個 migration：`001_initial`（案件／稽核／冪等）、`002_resources`（版本／註記）、`003_proposals`（證據／提案／合併）、`004_ai_runs`（run／job／事件）、`005_chat`（chat_threads／messages）、`006_case_documents`（上傳的原始 PDF）。**新表加新的 `00N_*.sql`，不要改已存在的 migration 檔。**

## 3. 不要打破的約定

這幾條是階段 A 的設計決定，違反會讓已通過的驗收案例失效。

1. **交易邊界在路由層。** service 的寫入函式假設呼叫端已經開好交易。所有會改狀態的端點都走 `api/mutation.py` 的 `execute_mutation`，它負責：冪等查詢 → 單一交易內執行 operation → 在同一筆交易寫入冪等回應。不要在 service 裡自己開交易。
2. **每個請求一條 SQLite 連線**（`api/deps.py` 的 `get_db`）。不要改成共用單一連線，sqlite3 連線不是執行緒安全的。
3. **不要用 `executescript` 包在 `transaction()` 裡。** Python 的 `executescript` 會先隱式 commit，會把你開的交易關掉，接著 COMMIT 會報「no transaction is active」。migration 執行器因此刻意不開交易。
4. **`advance_case` 是推進版本的唯一入口**，它用 `WHERE case_revision = expected` 更新並檢查恰好一筆，同時寫 case_snapshot。不要繞過它直接 UPDATE cases。
5. **heads 的形狀**是 `{resource_id: {revision_id, kind, freshness}}`，存在 `cases.active_heads_json`。`freshness` 屬於目前指標，不屬於版本內容，所以不在 `resource_versions` 裡。所有 heads 操作（`set_head`、`mark_stale`）都回傳新的 dict，不就地修改。
6. **資源版本不可變。** 內容變動一律新增 `resource_versions` 一列，`parent_id` 指向前一版。註記的每次編修也是存成 `resource_kind='annotation'` 的新版本。
7. **人工文字編輯不解除 stale。** 只有 `POST /drafts/{id}/reviews` 帶理由才會把 freshness 改回 current。
8. **合併在預覽階段算完，採用交易只複核。** `merge_service` 產生不可變的 `merge_previews` 一列與 `preview_hash`；`apply_service` 只驗 preview 歸屬、案件版本、hash、組依賴閉包與是否已套用，然後寫版本。昂貴的計算不要搬進採用交易。
9. **有衝突時預覽的候選內容要保留目前版本。** `_Merger` 在衝突分支也會把 current 內容放進 `working`，否則人工整合端點會找不到資源可改。
10. **錯誤碼照 [04 §7](04-API契約.md)。** 已用到的對應：缺欄位或欄位不合規 422 `INVALID_FIELD`；壞 JSON 400 `MALFORMED_REQUEST`；候選越界或基底不符 422 `OUT_OF_SCOPE_PATCH`；引用不存在或必要引用組沒一起採用 422 `INVALID_CITATION`；案件或資源版本過期 409 `REVISION_CONFLICT`；依賴事實已變 409 `DEPENDENCY_STALE`；區塊不在該版本 409 `TARGET_MOVED`；重複採用同組 409 `PROPOSAL_ALREADY_APPLIED`；終態 run 再取消是 409 `RUN_NOT_CANCELLABLE`；跨案與不存在一律 404 `RESOURCE_NOT_FOUND`。
11. **身分只從伺服器設定來。** `get_actor_id` 讀 Settings，前端不能指定 actor。要換成真登入就改這個 dependency，不要讓 actor_id 進入請求體。
12. **測試風格**：Arrange-Act-Assert，測試名稱描述行為。新功能先寫測試（RED），再實作。`tests/conftest.py` 有 `mutate`、`create_case`、`create_draft`、`proposal_body` 等 helper，冪等鍵預設自動產生，要測重送才自己指定同一把。**`TestClient` 綠燈不等於真的能跑。** 2026-09-13 發現：`caseapi/db/connection.py` 的 `connect()` 原本沒加 `check_same_thread=False`，83／92／104／122 項測試全數通過都沒發現，因為 Starlette 的 `TestClient` 不會重現真正用 `uvicorn` 跑時，FastAPI 把同步 generator dependency（`api/deps.py` 的 `get_db`）的建立與收尾分派到 anyio threadpool 不同 worker thread的行為；直到第一次真的啟動 `uvicorn` 並用瀏覽器打 `GET /cases/{id}` 才炸出 `sqlite3.ProgrammingError`。**新增會被前端直接呼叫的端點時，除了 `pytest`，至少手動啟動一次真正的 `uvicorn` 並用瀏覽器或 `curl` 打過一輪，不能只看 `TestClient` 綠燈就當作驗證完成。** 修法與重現測試在 `tests/test_db.py::test_connection_survives_being_closed_from_a_different_thread`。
13. **run context 建立後凍結；事件只能由伺服器執行層追加。** `run_events.sequence` 在每個 run 內單調遞增，重新讀取用 `after_sequence`，不重跑工作。`provider_config_json` 只存 provider／model 等非秘密設定，service 會拒絕 credential 類欄位。
14. **search 與 open 不是同一證據狀態。** `search_knowledge` 只寫 `tool.started/completed`，不寫 evidence；只有 `open_source` 成功時能在同一交易寫 `source.opened` 與 `assessed_by=program` 的 evidence。這只證明來源存在與 quote 一致，`support_status` 仍為 `unknown`。
15. **`ModelProvider`／`ToolGateway` 是 provider 換血的唯一邊界。** `execute_chat_run`（`ai/runner.py`）只認 `caseapi.ai.contracts.ModelProvider` 這個 Protocol；`ToolGateway` 把 `case_id`／`run_id` 綁死，provider 不能指定別的案件。接線上 provider（AgentCore／Bedrock）時新增一個實作這個 Protocol 的類別即可，**不要改 runner 或 routes_chat 去繞過這層**，也不要讓 provider 直接拿到 DB 連線或案件 repository。`descriptor()` 回傳的字典只能放 provider／model／region 這類非秘密設定；憑證一律在 provider 內部用 `os.environ` 現讀現用，不進 `provider_config_json`（見規則 13 的 credential 拒收）、不進 log。
16. **`fail_run` 對已終態的 run 是 no-op，不是錯誤。** 這是 B3 收尾修的一個真競態：run 被使用者取消後，若背景中的 provider 仍在同一次 `execute_chat_run` 裡把結果寫回，`complete_run` 會因狀態已非 `running` 而丟出例外；原本的例外處理再呼叫 `fail_run`，若它也對非 `running` 狀態丟例外，第二個例外沒人接住，會直接穿出背景工作（Starlette 的 `BackgroundTasks` 不會幫你接）。現在 `run_service.fail_run` 對已在 `TERMINAL_STATES` 的 run 直接回傳現況，不改狀態也不重覆寫事件。**`fail_run` 只由 `ai/runner.py` 的例外處理呼叫，不是使用者可觸發的 API，之後新增呼叫點前要重新確認這個假設還成立。** 對應測試：`tests/test_fixed_model_e2e.py::test_cancelling_a_running_run_survives_a_provider_that_finishes_late`，重現方式是讓假 provider 在 `execute()` 內部直接呼叫 `run_service.cancel_run` 模擬使用者在模型思考時取消。

17. **草稿生成不改正文，只產生提案。** `POST /cases/{case_id}/draft-generations` 在路由的單一交易裡凍結這次生成的基底：案件還沒有草稿時先存一份 `origin=program`、只有一個「（尚未生成內容）」區塊的空殼草稿（`freshness=stale`），讓 `replace_document` 有合法的 target；已經有草稿就重生那一份，不再建第二份。背景 run 結束時走既有的 `proposal_service.create_proposal`，提案 `state=ready`，正文要等人工採用（merge preview → application）才會變。**不要為了 demo 方便讓 run 自動採用提案**——那會繞過範圍驗證與三方合併，也讓 V01／V02 的衝突保護失效。
18. **`draft` intent 的法規來源只有人工選定的那批。** provider 不再自己 `search_knowledge`；它對 `statute_selection` 裡的每個 chunk 呼叫 `open_source`，拿到的 evidence id 就是提案修改組的 `evidence_ids`（規則 14：搜尋是候選，開原文才是證據）。沒選法規就不生成，回一句說明，不編一份沒有依據的草稿。
19. **`ModelRequest.context` 是伺服器讀好的資料，不是連線。** `ai/draft_context.py` 依 run 凍結的 `facts_revision`／`statute_selection_revision`／`appeal_document_id` 讀內容，所以同一個 run 重跑讀到同一份 context；provider 仍然拿不到 DB 或 repository（規則 15 沒被打破）。
20. **線上草稿 prompt 固定，輸出還要過本機安全閘門。** `draft` intent 不把 API 傳入的自由 instruction 當 AgentCore 任務；只使用 `AGENTCORE_DRAFT_PROMPT`，明定訴願書是未確認主張、忽略 context 內嵌指令、禁止補機關／新事實與最終決定。回傳理由仍須經 `guard_model_reasoning`：命中受理機關、決定主文、駁回／不受理／有無理由／撤銷或維持原處分等高風險標記時整段換成阻擋訊息，不把原始模型字串寫入提案；中性文字一律標「未經法律覆核」。這只是已知風險的 deterministic gate，不得宣稱能證明一般事實或法律正確性。

## 4. 已知偏離與待回寫契約

這三個端點的實作語意已回寫 [04](04-API契約.md)，但前兩個仍是階段 A 為了驗證流程而加的暫時入口，不等於正式 run 流程：

| 端點 | 為什麼加 | 之後怎麼辦 |
|---|---|---|
| `POST /api/v1/cases/{case_id}/drafts` | 沒有草稿就測不了編輯與 diff | **已退場（2026-09-13）**：OpenAPI 標 `deprecated`，前端不呼叫；正式流程走 `POST /draft-generations`。保留只為版本／合併測試建初始草稿 |
| `POST /api/v1/cases/{case_id}/proposals` | 契約的提案由 run 產生，那是階段 B 之後 | **已退場（2026-09-13）**：`draft` intent 會產生真提案，這個入口標 `deprecated`，只剩 merge／apply 測試在用 |
| `POST /api/v1/cases/{case_id}/drafts/{draft_id}/reviews` | 保存一個新版本並把 freshness 改回 current | 已與 04 對齊；規則引擎完成後再加入必要分析未完成時的阻擋 |

其他缺口：

- **兩個 provider 現在支援 `verify`／`explain`／`draft`／`revise_selection` 四個 intent。** `draft` 與 `revise_selection` 的結果都走 `proposal_service` 產生可人工採用的提案（見 §3 規則 17–20 與 §7.3）；前者是完整文件候選，後者把 target 鎖在使用者選取的字元範圍。
- **完整草稿提案已有採用與編輯畫面。** `DraftGenerationPanel.vue` 先呼叫 `merge_service` 的 preview，確認 `can_apply` 才呼叫 `apply_service`；`DraftEditorPanel.vue` 讀 draft head 並逐段 PATCH。完整文件衝突會阻擋並保留人工內容，但 `resolutions` 的人工整合 UI 尚未接。
- **背景協調器是 Starlette `BackgroundTasks`，不是可恢復 worker。** 服務中止時 running 的 run 永遠停在 running，`lease_until` 過期也沒有回收機制。demo 錄影如果會重啟後端，這個要先補一個啟動時掃描過期 lease 並標記 failed 的步驟，否則畫面會留下卡住的轉圈。
- **SSE 是「補送已存事件後關閉」，不是等待新事件的長連線。** `GET /runs/{id}/events?format=sse` 目前行為等同一次性把 `after_sequence` 之後的事件轉成 SSE frame 就結束串流；瀏覽器要看到「思考中→查詢中→完成」的漸進效果，需要前端自己用假的時間軸配合這批已存事件呈現，不是後端真的在串流。使用者已確認 demo 走錄影、前端動畫可以是假的時間軸，但畫面上列出的「查了哪些資料」必須來自真事件，不能編造。
- **Evidence adapter 的查證寫入路徑已由 fixed run 使用，但 support_status 仍是 `unknown`。** `open_source` 只證明來源存在與 quote 一致，不判斷是否支持本案主張；線上 provider 也不該假裝能回答這件事，除非另外設計一個明確的法律判斷欄位並要求人工覆核。
- **同一區塊的字元級自動合併沒做**，依 [03 §6.1](03-後端SQLite與版本合併.md) 規則 3 保守視為衝突。完整文件衝突也是整份判定。
- **沒有 `PATCH /selections`**，引用是否齊備只靠修改組依賴閉包把關。
- **`gate_result` 與 `source_span` 兩種 TargetRef 只驗形狀**，不驗目標存在，因為沒有 analyses 與來源快照表。
- **沒有完整法律規則引擎。** 第77條八款已補條件分支，但未涵蓋假日、在途期間及所有法律例外；法律性質與資格仍需覆核。事實變動仍保守把所有草稿標 stale，這是 H01 只能標 `PASS＊` 的原因。
- **舊後端仍獨立存在，但上傳、規則式抽欄位、期限試算、選法規、草稿與 PDF 匯出主流程都已搬到新後端。** 現在保留 legacy 的直接理由是 `/legacy-demo` 與 Word 匯出；新版 PDF 已完成驗收，但 DOCX 是否保留尚未決定，見 §7.5。

## 5. 前一輪：五個前端畫面與線上 provider（全部已完成）

使用者已把整個產品的 demo 範圍講清楚：訴願書格式固定用規則式抽欄位（不要換成模型抽取，模型在固定格式上只會多一個幻覺來源）；行政處分函格式不固定，AI 抽取可以是加分項但做不完可以先擱置；77 條那類程序審查目前只有期限提醒的邏輯，法律正確性沒有人覆核過，前端不能顯示「系統認定不受理」這種斷言句，只能呈現檢查結果並標明未經法律覆核；前端要整個換成新後端，但**逐畫面改接，每改完一步都要能繼續錄 demo**，舊後端與舊前端的 demo 選單等新後端補完對應能力、且前端全部改接完成後才刪除，中途不要為了「乾淨」而先斷線。

### 5.1 線上 provider（已完成，2026-09-13）

`caseapi/ai/agentcore_provider.py` 的 `AgentCoreModelProvider` 已經接上真的
AWS Bedrock AgentCore Runtime，並用真實請求跑過一次完整的 FastAPI → 工具
→ AgentCore → assistant 訊息鏈路，run 狀態 `completed`。細節、已部署資源的
名稱／ARN、怎麼重新部署、怎麼切換 provider、兩個部署時踩到的坑，全部寫在
[07 AgentCore 部署與線上模型](07-AgentCore部署與線上模型.md)，不要在這裡
另外複製一份。摘要：

- 本機真的做 BM25 檢索與 quote 驗證（沿用 `EvidenceToolAdapter`），只把已驗證的內容當 `context` 送給 AgentCore 生成最後一段文字；AgentCore 拿不到 DB 連線或案件 repository（規則 15 沒被打破）。
- `CASEAPI_MODEL_PROVIDER=fixed|agentcore` 切換 provider，預設仍是 `fixed`，斷網或 AWS 憑證過期時 demo 仍能完整跑完。
- 原本規劃要讓 provider 在工具呼叫之間檢查 run 是否已取消；重新檢視後放棄這個設計，因為那需要 provider 拿到 DB 連線，直接違反規則 15。改成依賴已經修好的 `fail_run` 冪等性（規則 16）保證正確性，與 provider 實作無關。
- 測試（`tests/test_agentcore_provider.py`、`tests/test_model_provider_selection.py`）全部用假的 `ToolGatewayLike`／boto3 client，CI 不連外網；`backend/scripts/deploy_agentcore.py` 是唯一會打真實 AWS API 的地方，不在 pytest 範圍內。

### 5.2 前端能上場的八個項目（依使用者指定順序，逐項獨立 RED／GREEN／commit）

本節保留完成當時的實作快照與瀏覽器證據；2026-09-13 後來加入的八款審查契約、
進件衍生分析、精簡查詢／完整法條結果與正式草稿骨架，以 §7.4a 為準。下列「只做期限」、
「行政處分函不分析」及「三個草稿區塊」等句子描述的是當批狀態，不是現況。

1. **上傳建案（已完成，2026-09-13）。** `POST /api/v1/cases/intake`（multipart：`appeal_pdf` 必填、`disposition_pdf` 選填、`title` 選填）建立案件、把兩份 PDF 存進新的 `case_documents` 表（含原始位元組，供之後「原檔展開」）、用 `caseapi/domain/appeal_extraction.py` 對訴願書全文做規則式抽取，把抽到的欄位寫成 facts 資源的第一版（`origin='program'`）。**只抽訴願書，不抽行政處分函**：訴願書是固定格式的行政院標準表單，逐案校準過 6 份 `data/raw/訴願書予行政處分函-1/` 真實樣本全部正確；行政處分函格式不固定，存原件但不抽欄位，交人工或之後另外授權的 AI 輔助抽取。抽不到的欄位（`appellant.address`、`service.date`、`service.method`）留 `None`，不猜。`GET /api/v1/cases/{case_id}/facts` 一併補上，讀目前 facts 欄位。
2. **案件列表（已完成，2026-09-13）。** `CaseSummary`／`CaseDetail` 新增 `processing_status`（`unprocessed`／`processing`／`completed`），在 `case_service._processing_status` 讀取當下依 heads 有沒有 draft、audit_entries 有沒有 `draft.reviewed` 動作即時算出，沒有新增欄位或狀態機表。
3. **原檔展開（已完成，2026-09-13，原本是階段四的範圍，因為案件列表點進去需要有地方去而提前做）。** `GET /api/v1/cases/{case_id}/documents`（列表，不含 blob）與 `GET .../documents/{document_id}/content`（回傳原始 PDF bytes，`Content-Type: application/pdf`，跨案或不存在一律 404）。前端 `CaseDetailView.vue` 用 `<iframe>` 嵌瀏覽器原生 PDF viewer 顯示，兩份文件用 tab 切換，不做自製的 PDF renderer。
4. **前端首頁與案件詳情頁（已完成，2026-09-13）。** `frontend/src/views/HomeView.vue` 取代原本的五步驟精靈當首頁：案件卡片列表＋狀態標籤＋「上傳新案件」對話框，拿掉了 `demoCases.ts` 的「選 demo」入口（該檔案還在，只是新首頁不再引用）。`CaseDetailView.vue` 顯示抽取事實與原始文件。舊的五步驟精靈搬到 `/legacy-demo`（路由名稱仍是 `wizard`），繼續走舊後端 `/api/*`，兩邊不共用狀態；`vite.config.ts` 新增 `/api/v1` proxy 指到新後端（`:8001`），排在舊的 `/api` proxy（`:8000`）之前。
5. **側邊欄 AI 對話（已完成，2026-09-13，原本排在程序審查後面，因為後端已經全部做好、風險最低而提前）。** `frontend/src/components/ChatSidebar.vue` 掛在 `CaseDetailView.vue`，右下角浮動按鈕開關。純前端接線，B3／AgentCore 的聊天後端一行都沒改：送訊息、輪詢 `GET /runs/{id}`（每 600ms，最多 40 次，沒有真 SSE）、完成後讀 `GET /runs/{id}/events` 把 `tool.started`／`tool.completed` 配對成人看得懂的一行（`frontend/src/utils/runEvents.ts`，有單元測試），再讀 `GET messages` 拿真的 assistant 回覆。**思考中／查詢中只是前端假的節奏，顯示的每一行「查了什麼」都來自真事件**，事件裡沒有對應的 `tool.started` 就不猜、不畫那一行（見 `runEvents.spec.ts` 的測試）。事實欄位旁加了「問 AI」按鈕，用 `fact_field` 目標打 `explain` intent——這連帶修了一個真的缺口：`FixedModelProvider`／`AgentCoreModelProvider` 的 `_explain` 原本只認 `draft_block` 目標的 `selected_text`，對 `fact_field` 目標永遠讀到空字串，因為欄位值其實在 `read_selection_context` 回傳的 `context.field` 裡，不在 `writable_target` 上；新增 `caseapi/ai/selection_text.py` 統一處理兩種目標，兩個 provider 都改用它。真瀏覽器手動測過 verify（自由提問）與 explain（點欄位問 AI）兩條路徑，AgentCore 都給出誠實、有依據或明說查不到的回答。
6. **程序審查（已完成，2026-09-13；範圍是訴願法第14條三十日期間試算,不是完整的第77條各款不受理事由）。** `GET /api/v1/cases/{case_id}/procedural-review` 讀取當下 facts 即時算出，不持久化、不建 `analyses` 表——跟 `processing_status` 同一種作法。`caseapi/domain/appeal_deadline.py` 是純函式：`service.date` 缺就回 `insufficient_data`，**不拿 `disposition.date` 頂替**——處分日期不等於送達日期,用它算會低估期限,可能把還沒過期的案件誤判成逾期,這個風險不值得為了少一次人工填值去冒；`service.date` 有、`appeal.filed_date` 沒有就回 `deadline_known_filing_unknown`（給期限但不判斷是否逾期）；兩者都有才回 `within_period`／`overdue`。只算日曆天數,不處理國定假日順延（行政程序法§48 IV 需要假日曆,沒有就在 `caveats` 裡明說,不裝懂）。回應固定帶 `legal_review_status: 'not_reviewed'`，contract 測試斷言輸出字串裡不能出現「不受理」。前端 `ProceduralReviewPanel.vue` 掛在 `CaseDetailView.vue`，缺欄位時給一個小表單直接呼叫既有的 `PATCH /facts`（走 `human_asserted: true`），不是新的寫入路徑。四種狀態都用真瀏覽器對真後端測過（`insufficient_data`→`deadline_known_filing_unknown`→`overdue`→`within_period`），逾期天數與期限日期都對得上手算結果。**`GateResultTarget`／AI 側邊欄的 explain 沒有接這個結果**：那需要一張快照表才能讓目標的 `analysis_id`/`gate_id` 可驗證存在（見 §4 那條「只驗形狀不驗目標存在」的舊缺口），這次為了不新增表格所以沒做，留給下一次真的要在側邊欄解釋程序審查結果時再處理。第77條其餘各款（其他不受理事由）也還沒做，只做了期間這一款。
7. **選法規（已完成，2026-09-13）。** `GET /cases/{case_id}/statute-search?q=`（可省略 `q`，省略時用 `caseapi/domain/appeal_extraction.py` 新增的 `extract_case_narrative` 從訴願書事實／理由段落抽出查詢字串，不是整份文件也不是憑空生成）對 r3 語料做真的 BM25，`document_types={'statute'}`。**搜尋本身不寫 evidence／run 事件**——這是人工瀏覽候選，不是 AI 工具呼叫；之後真的拿去當草稿依據才需要走 `EvidenceToolAdapter.open_source` 補上 program-verified 的驗證，見規則 14。人工挑選結果存成新的資源種類 `statute_selection`（`case_repository.KIND_STATUTE_SELECTION`），沿用既有 `resource_versions`／heads／`advance_case` 機制，`PUT /cases/{case_id}/statute-selection` 整批覆蓋、不是逐筆增刪，跟 facts 的欄位式 PATCH 不同語意。前端 `StatuteSelectionPanel.vue` 掛在 `CaseDetailView.vue`，搜尋框預先帶入建議查詢、可自行修改重新搜尋。真瀏覽器對真後端測過：預設查詢字串正確抽出案件的事實理由段落，搜尋結果第一筆就是案件理由裡實際引用的洗錢防制法第6條，加入、儲存、重新整理後仍保留都驗證過。
8. **草稿生成（已完成，2026-09-13）。** `POST /api/v1/cases/{case_id}/draft-generations`（202＋背景 run，`kind='regenerate'`、`job_kind='draft.run'`）觸發新的 `draft` intent，`FixedModelProvider` 與 `AgentCoreModelProvider` 都實作。context 來自 run 凍結的 refs：目前事實（facts 版本）、選定法規（`statute_selection` 版本）、訴願書 `extracted_text`（見 `ai/draft_context.py`）。provider 對每條選定法規呼叫 `open_source` 取得 program-verified evidence，再組出三個區塊（`fact-1` 事實／`statute-1` 依據／`reason-1` 理由）；只有理由段落由模型寫，事實與法規原文都是本機驗證過的內容。AgentCore 的草稿任務固定且不能被 API instruction 覆寫，模型輸出還要通過 §3 規則 20 的本機閘門；安全中性文字仍標示未經法律覆核。結果經 `proposal_service.create_proposal` 變成 `mode=full`、單一 `structure` 修改組、`replace_document` 的真提案，`evidence_ids` 指向剛才開啟的來源，**正文不變**。前端 `DraftGenerationPanel.vue` 掛在 `CaseDetailView.vue` 選法規面板下方：先列出「會用到的事實／會引用的法規」（真資料，沒選法規時按鈕停用），按下去顯示處理中節奏（假時間軸）＋真的 run 事件活動紀錄，完成後顯示提案內容並明說尚未採用。安全修補後以 fixed provider、真 `uvicorn`＋前端 dev server＋Playwright 重跑案01上傳→選洗錢防制法第6條→生成，結果持續顯示且 console 0 error；修補後 AgentCore 真實重測因真實訴願內容外送未獲明確授權而記為 NOT RUN。程序審查的期限試算**沒有**放進 context——那是法律內容判斷，沒有跟使用者確認過，寧可不放。採用提案與編輯草稿已由 §7.2 完成；仍未做的是 §7.3 的局部改寫。

5.1、5.2 都完成了，但 [05 §3](05-實作順序與驗收.md) 的 A05、A06、A07 仍然是 NOT RUN——`draft` 只產生單一完整候選，沒有可分開採用的修改組，也還沒判斷來源支持程度，理由寫在 05 §3 表格下方。**改狀態時要附實跑證據，不能因為程式寫好了就標 PASS。**

**上面 1–8 項與 §7.2 除了 `pytest` 之外，也真的啟動 `uvicorn` 與前端 dev server，在瀏覽器裡走過上傳→開啟案件→切換原始文件 tab→側邊欄對話→程序審查四種狀態→選法規→產生草稿→採用→人工編輯→reload 的流程**——1–4 項撞見並修好了規則 12 的 threading bug，第 8 項撞見生成結果被父頁重新掛載清掉的 UI bug；§7.2 又驗證無衝突 application 與 `SAME_DOCUMENT_CHANGED` 阻擋，並抓到 `origin=program` 空殼被錯標為合併版本的 UI bug。這些都不能只靠 `pytest` 綠燈宣稱完成。

## 6. 已決定事項與剩餘授權

1. **語料固定讀 r3。** r1/r2 保留為歷史證據，不作新證據層來源。r3 只索引 `index_eligible=true` 的 3,002 個 chunk；101 個歷史決定書主文 chunk 以 `historical_outcome_excluded` 排除。法規、標籤與時效仍受 [r3 稽核](../../sysdoc/verification/r3-audit.json) 的限制。
2. **先做 BM25，gold 與量測排在 demo 能跑之後。** 已完成 r3 離線 baseline、同案排除、去重與精確引文定位；不要先加向量。使用者已同意用 r3 語料裡 588 筆已解析引文自動生成 25–30 題 gold（不用人工標註），但排在整條 demo 流程能跑之後才做；不要因為要做評估而拖到前端進度。向量只有在量出 BM25 具體缺口才加。
3. **線上模型：AWS Bedrock AgentCore，且進件逐次同意。** 使用者要求後台看得到 AgentCore 被呼叫，不只是 Bedrock 模型端點；細節見 §5.1。憑證只讀環境（`.env` 已有），過期時如實回報，不自行更新使用者的憑證。部署設定為 AgentCore 只代表能力可用，不代表每份上傳都可外送；每次進件仍須由使用者勾選同意，預設只做本機分析。
4. **前端主流程已全面改接新後端；PDF 已完成，但 legacy 仍等 DOCX 決策後才刪。** 首頁是案件列表＋上傳入口，五步驟、選取叫 AI、草稿採用／編輯與 PDF 下載都已接新後端；舊流程只留在 `/legacy-demo`。PDF 已完成內容讀回與版面驗收，但 DOCX 是否仍需保留尚未釐清，因此不得刪 `backend/api.py`／`backend/src/` 或 legacy 前端，實際清單見 §7.5。展示上傳請優先使用 `data/demo/` 內同案的兩份虛構 PDF，不要把 `data/raw/` 真實樣本當成可任意外送的素材。
5. **身分維持單人 stub。** 目前只驗同一 actor 下案件隔離；多人權限與 V08 的跨使用者部分尚未驗證。若下一步需要正式登入或組織角色，必須另向使用者取得授權與需求。

## 7. 主流程、內容與 PDF 匯出已完成；確認 DOCX 需求後才決定刪除 legacy

§7.1、§7.2、§7.3、§7.4、本批 §7.4a 與 PDF 匯出已於 2026-09-13 完成；§7.5
（刪除舊後端）仍未開始。AWS v0.2.3 已原地發布，且使用者尚未決定是否保留
DOCX；在這兩件事各自完成前，不把 §7.5 當成可直接執行的下一步。

### 7.1 草稿生成（已完成，2026-09-13）

實作內容與限制見 §5.2 第 8 項與 §3 規則 17–20；實跑證據在 [05 §6](05-實作順序與驗收.md)。原本這一節的規劃如下，全部做到了，只有一項刻意沒做（程序審查結果沒有放進 context，理由見 §5.2 第 8 項）：

- 目前 `FixedModelProvider`／`AgentCoreModelProvider` 只認 `verify`／`explain`；新增
  intent 時兩個 provider 都要處理，且結果要透過 `proposal_service` 產生真正的
  proposal（有 `groups`／`EvidenceRef`，見 [04 §3](04-API契約.md)），不是像
  `verify`／`explain` 那樣把 `ModelResult.answer` 直接存成聊天訊息就結束。
- context 至少要包含：目前 facts（`facts_service.read_current_fields`）、選定的
  法規（`statute_selection_service.get_selection`，見 §5.2 第 7 項）、原始訴願書
  文字（`case_documents.extracted_text`）。程序審查的期限試算結果（§5.2 第 6 項）
  要不要放進去、放多少，這是法律內容判斷，本文件不替你決定，留給實際做的人跟
  使用者確認。
- **`POST /api/v1/cases/{case_id}/drafts` 這個階段 A fixture 入口該退場了**（見
  §4 表格）：真正的草稿建立應該由這個新 intent 觸發，而不是由前端直接呼叫
  fixture 端點生一個空白草稿。
- 前端需要一個「產生草稿」的觸發點（放在 §5.2 第 7 項選法規完成之後很合理）與
  一個處理中畫面——使用者原話是「選擇相關法規之後跳一個處理畫面，然後生成
  草稿」。這個畫面可以照 `ChatSidebar.vue` 已經驗證過的作法：假的時間軸節奏
  （思考中／生成中），但只要畫面上出現「參考了哪些法規／哪些事實」，就必須
  是真資料，不能編。

§7.1 產生的提案已由 §7.2 接上人工採用；尚未採用的提案在重新整理後仍沒有列表頁，
但已採用的 draft head 與其不可變版本會正常回讀。

### 7.2 手動編輯草稿（已完成，2026-09-13）

後端契約沒有新增；這一輪把階段 A 已有能力接到新前端：

- **7.1 產生的提案已接上採用鏈**：`POST /proposals/{id}/merge-previews` →
  `POST .../applications`。沒有這一步，生成出來的草稿永遠進不了正文，草稿 head
  會一直停在 `origin=program` 的空殼。
- `DraftEditorPanel.vue` 從 case 的 draft head 讀 `GET /resources/{draft_id}`，顯示
  blocks、origin 與 freshness；每段可就地編輯，保存走既有的
  `PATCH /drafts/{draft_id}`，並攜帶目前 case／resource revision。衝突時重新讀取最新
  head，不會用舊畫面靜默覆蓋。
- 無衝突的重新生成／採用已完成；若 proposal 生成後正文又被人工修改，merge preview
  會顯示 `SAME_DOCUMENT_CHANGED` 並停止，不呼叫 application。**尚未完成的是呼叫
  `POST .../resolutions` 的人工衝突整合 UI**；目前明確提示保留人工內容並重新生成，
  不假裝已支援逐段解衝突。
- 實跑證據見 [05 §6](05-實作順序與驗收.md)：案01真 PDF、fixed provider、真
  uvicorn／Vite／Playwright 走過採用、編輯、reload 持久化與衝突阻擋，console 0 error。

### 7.3 選取文字叫 AI——先解釋，再做修改與 diff／接受／拒絕（已完成，2026-09-13）

實作內容見 [05 §6](05-實作順序與驗收.md) 新增的兩個批次列與其後的實跑證據段落；原本規劃的重點全部做到：

- **7.3a 解釋**：後端不用改（`_explain` 本來就認得 `draft_block`）。新增
  `frontend/src/utils/draftSelection.ts` 做 UTF-16→Unicode code point 轉換與
  `sha256(UTF-8 selected_text)`；`DraftEditorPanel.vue` 用每個段落的
  `selectionStart`／`selectionEnd`（不是 `window.getSelection()`）取得範圍，有反白時使用
  精準範圍，沒有反白時以整段為目標；`ChatSidebar.vue` 的 `send()` 泛化成接受 `TargetRef`（`FactFieldTarget`
  ∪ `DraftBlockTarget`），新增 `explainSelection()`。
- **7.3b 局部修改、diff、接受、拒絕**：契約早就在 [04 §4](04-API契約.md) 定好形狀——
  intent 叫 `revise_selection`，走既有 `POST chat-threads/{id}/messages`；新增
  `caseapi/ai/revise_selection.py`（兩個 provider 都實作）、
  `caseapi/services/revise_selection_service.py`（產生 `mode='local'`／`replace_text`
  提案，頂層 target 與操作 target 用同一個 `DraftBlockTarget`，範圍檢查天然通過）、
  一個新 migration 放寬 `messages.intent` 的 CHECK constraint。候選文字共用 `draft`
  intent 的危險字樣阻擋（抽成 `draft_composition.unsafe_reasoning_markers`）。前端
  `ChatSidebar.vue` 新增「附加選取待修改」的 pending 狀態＋diff 卡片＋接受（沿用
  merge preview → application）／拒絕（`POST .../rejections`）；`DraftEditorPanel.vue`
  多一顆「請 AI 修改此段」按鈕。
- **純 HTTP 相容性與單一編輯介面（2026-09-13 修正）。** 公開 HTTP 頁面沒有
  `crypto.subtle`，舊版會在建立 `DraftBlockTarget` 時丟錯，使兩顆 AI 按鈕看起來完全
  沒反應；現在 `draftSelection.ts` 有已用標準向量驗證的 SHA-256 相容路徑。草稿採用後，
  proposal 區只保留「草稿已建立」狀態，不再重複正文或顯示內部 proposal ID；正式正文
  的每一段都能直接切換「編輯／儲存／取消」，未保存時 AI 與 PDF 操作會停用。
- A01（同文句第二次出現只改第二次）已由真的 `revise_selection` run 端到端證明；
  真瀏覽器對真後端測過選第二次出現的片語→送修改指示→看到 diff→接受（正文只變
  選取範圍）、以及另一次選取→送出→拒絕→reload 無殘留待採用狀態。AgentCore 的
  `revise_selection` 只在 provider 層用假 client 測過安全閘門，未對這批真實訴願內容
  重跑線上模型（沒有外送授權），線上實跑維持 **NOT RUN**。

### 7.4 UI 打磨——對齊 `docs/design` 的五步驟流程（已完成，2026-09-13）

`CaseDetailView.vue` 已改寫成五步驟 wizard（進件上傳／擷取與解析／程序審查§77／
法條與前例／決定書草稿），細節與實跑證據見 [05 §6](05-實作順序與驗收.md) 新增的
四個批次列。與原規劃的差異或需要注意的地方：

- 步驟切換用 `v-show`，不是 `v-if`：`DraftGenerationPanel`／`StatuteSelectionPanel`
  等面板自己在 `onMounted` 抓資料並持有暫時性狀態（例如剛生成但未採用的提案），
  `v-if` 會在切步驟時把它們卸載重建、白白遺失那份狀態。**之後在這個 wizard 裡新增
  步驟或面板，維持 `v-show`，不要改回 `v-if`。**
- 步驟解鎖完全由既有資料推導（文件存在／`statute_selection` 非空／草稿 head
  存在），沒有新增後端欄位；只有「擷取」「程序審查」兩步沒有可用的「已確認」資料
  訊號，用當次瀏覽階段的確認（點下一步即確認），reload 後如果使用者已經做到更後面
  一樣會直接解鎖到那裡，不會打回頭——這是刻意的取捨，不是遺漏。
- 訴願法第77條八款（本節完成當時的狀態，後由 §7.4a 取代）：新增 `frontend/src/utils/article77.ts`（標題取自
  `docs/design/Gate.dc.html`，本專案既有設計參考稿），只有第2款用既有真實試算，其餘
  七款固定「待人工確認／尚無自動判定規則」，不宣稱不成立／不適用／查過案件庫。
- 移除了 `AppHeader.vue` 原本無條件顯示在所有路由的假承辦人姓名徽章（現在只在
  `/legacy-demo` 顯示）；`/legacy-demo` 本身與其檔案沒有動，正式刪除仍依 §7.5。
- 撞到一個 vitest／jsdom 的坑：`wrapper.isVisible()` 在同一測試裡兩個相鄰 `v-show`
  目標交替切換時會回傳對調的結果，改成直接讀 inline style 字串；細節見
  `frontend/src/views/__tests__/caseWizard.spec.ts` 的註解，**這是測試環境的限制，
  不是 Vue 或這個元件的 bug，之後別再花時間去「修」v-show 本身**。

### 7.4a 本批內容完善（已完成，2026-09-13）

- **第77條八款不再只有前端靜態說明。** `caseapi/domain/article77.py` 對每款輸出
  `rule_id`、輸入快照、規則說明、理由、狀態與 `evaluation_mode`。第2款沿用第14條
  日期試算，並完整涵蓋第57條但書所定期間內未補送訴願書的另一分支；只證明仍在
  第14條期間內、但缺少第57條通知／補送事實時，必須回 `INSUFFICIENT_EVIDENCE`，
  不能誤報 `NOT_TRIGGERED`。第1／4／5／6／7款是規則輔助但只回
  `INSUFFICIENT_EVIDENCE`；第3／8款固定 `manual_review`＋`NEEDS_HUMAN`。這些人工
  或證據不足的款不能回 `TRIGGERED` 或 `NOT_TRIGGERED`，避免輔助規則冒充法律判定。
- **擷取與解析增加三個可編輯的衍生 facts。** `analysis.keywords`、
  `disposition.summary`、`analysis.statute_query` 在 intake 時由目前分析路徑產生並保存。
  每一次上傳都要由使用者明確勾選 `consent_to_online_analysis` 才能把該次文件文字送往
  AgentCore；核取方塊預設未勾選，沒有同意時一律用本機分析。線上呼叫在 SQLite 寫入
  交易之外執行；逾時、例外或格式錯誤時會以一般化錯誤訊息回報、改用本機分析，案件
  與原始檔仍正常保存。三者都保留實際 provider、`origin=llm` 與
  `legal_review_status=not_reviewed`；人工保存後改為 `origin=human`，但仍不自動升級為
  法律覆核通過。前端使用產品語彙呈現，技術 provenance 留在 API／資料層供稽核。
- **法規搜尋把人類查詢與檢索查詢分開。** 使用者看見、可修改的是最多 80 字的
  `analysis.statute_query`；預設 BM25 仍在伺服器內使用較完整的訴願事實／理由，長字串
  不放進回應。每筆候選會打開完整 r3 section，顯示規則式 `why_relevant`，並依 11 部
  收錄法規的 pcode 白名單產生 `law.moj.gov.tw/LawClass/LawSingle.aspx` 官方條文連結；
  相關性說明與官方連結都不是「法條適用本案」的法律判斷。保存選擇時，客戶端只能
  指定 r3 `chunk_id`；伺服器會重新 `open_source` 驗證 span，並以 repository 的 canonical
  文件名、條號、完整原文、來源 hash 與官方 URL 重建內容。未知 chunk 回 422，客戶端
  偽造的法規名稱／條號／摘要不會進入資源版本或後續草稿。
- **草稿骨架套用 `docs/Reference/訴願書生成指南.md`。** 區塊順序為資料庫索引表頭、
  正文標頭、當事人、前言、主文、理由、結論銜接、署名、救濟教示、日期；只有人工
  指定 `decision_path=substantive` 才在主文後加入獨立「事實」。目前沒有人工核定的
  主文、結論、救濟與日期，所以保留可見占位，不以模型補出受理機關或訴願結果。

最終本機全套自動驗證為後端 230 passed／`caseapi` aggregate coverage 95%，前端 70
passed，type check、production build、oxlint、eslint 全部 PASS。新增回歸涵蓋第77條
第二款完整分支、canonical statute metadata、逐次同意與線上錯誤回退，以及 HTTP
origin 上傳。AgentCore 的 provider 契約已測；是否真的外送由每次進件核取方塊決定，
不能用部署層設定替代使用者同意。AWS v0.2.3 已以虛構文件完成 consent=true 的
AgentCore live 進件、段落 AI 操作與 PDF 下載驗收；raw 真實文件沒有外送。

### 7.5 刪除舊後端——PDF 已完成，仍須確認 DOCX 需求再刪

新後端已提供 `GET /cases/{case_id}/drafts/{draft_id}/pdf`，且前端能從已保存的草稿正文
下載；A4 尺寸、多頁、頁碼、中文字型嵌入、文字抽取、渲染頁面，以及跨案／舊 head／
檔名與字元錯誤拒絕均已驗收。匯出層會排除檢索用 `index-header-1`，並把法規原文的
孤立數字項次改排為「第 N 項」，避免重複標頭與多一張短尾頁。未保存的前端修改會停用下載。
這仍不代表使用者已放棄 DOCX，也不等於舊 `/api/draft/docx` 已有新版等價替代。刪除舊
後端前必須先確認 DOCX 是否仍需保留，並完成引用掃描；不要把 PDF PASS 當成 legacy
可以立即移除的授權。

真的要刪的時候，這些是目前確定不再被新前端引用的檔案／目錄（刪除前用
`grep -rn` 確認新前端真的沒有任何 import）：

- 後端：`backend/api.py`、`backend/src/`。
- 前端：`frontend/src/views/WizardView.vue`、`frontend/src/views/steps/`、
  `frontend/src/components/StepNav.vue`、`frontend/src/stores/case.ts`、
  `frontend/src/data/demoCases.ts`、`frontend/src/types/appeal.ts`（若確認無其他
  引用）、`frontend/src/api/client.ts`（舊後端專用的 client，跟 `caseapi.ts` 不是
  同一個）。
- `frontend/src/router/index.ts` 的 `/legacy-demo` 路由、`frontend/src/App.vue`
  裡 `isLegacyWizard` 那整段條件邏輯、`frontend/vite.config.ts` 的 `/api`（舊）
  proxy 條目——只留 `/api/v1`。
- 確認 `backend/requirements.txt` 裡只有舊後端在用的套件（例如 `python-docx`，
  除非 DOCX 匯出功能被重做並沿用它）可以一併清掉。

## 8. 專案硬規則

- **這是 demo 導向的專案，不要過度工程。** 使用者明確說過只需要能 demo。只實作當批真正用到的表與端點；保留規格裡便宜又被驗收表點名的機制（樂觀鎖、冪等鍵、稽核鏈），砍掉昂貴而 demo 看不到的。
- **畫面內容不能造假，時間軸可以。** 前端的進度動畫（思考中／查詢中／生成中）用假的時間軸沒問題，但顯示的「查了哪些資料」必須對應真實的 run 事件，不能前端自己編一段看起來合理的文字。
- **產品文案與技術 provenance 分層。** 一般使用者介面不顯示 `mock`、`demo`、provider 名稱等實作字眼；API、資料庫、測試與交接文件仍必須如實保留 `fixed`／`agentcore`、`evaluation_mode`、`not_reviewed`，不能為了畫面完整感刪除稽核資訊。
- **AgentCore 外送必須逐次取得同意。** `consent_to_online_analysis` 預設為 false；無同意不得呼叫外部 provider。外部呼叫不得放在 SQLite 寫入交易內，失敗時保存案件並回退本機，錯誤回應不得帶出外部服務原文或文件內容。
- **公開 AWS 入口目前不是正式安全邊界。** `http://54.186.135.15` 由 Elastic IP 固定，v0.2.3 已在同一 EIP 完成 live health／upload／UI／PDF smoke；但入口仍只有 HTTP、來源 IP allowlist、沒有登入，不得宣稱 TLS、身分驗證或多人隔離已完成。
- 不要重建或覆寫 `data/raw/` 原 PDF、`data/processed/releases/r1`／`r2`／`r3`、`backend/data/kb/` 舊語料；任何前處理實驗使用新 release ID。
- 舊 `backend/api.py` 與 `backend/src/` 依 §6.4 的順序汰換，改接完成前不要為了整併去動它們；真的要刪的時候照 §7.5 的清單與 DOCX 匯出確認事項。
- 程式風格見專案根目錄的 [AGENTS.md](../../AGENTS.md)：Python 四空格、PEP 8、`snake_case`、公開邊界加型別註記；檔案小而聚焦，函式不超過 50 行；不就地修改資料結構。
- 文件不等於能力。實作進度一律回寫 [05](05-實作順序與驗收.md)，並附實跑結果。
